name: Deploy AV Tree Pros to S3

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Verify AWS CLI
        run: aws --version

      - name: Upload images and static assets
        run: |
          aws s3 sync ./images s3://${{ secrets.S3_BUCKET }}/images \
            --cache-control "public, max-age=31536000" \
            --delete

      - name: Upload CSS files
        run: |
          aws s3 sync ./css s3://${{ secrets.S3_BUCKET }}/css \
            --cache-control "public, max-age=86400" \
            --delete

      - name: Upload JavaScript files
        run: |
          aws s3 sync ./js s3://${{ secrets.S3_BUCKET }}/js \
            --cache-control "public, max-age=86400" \
            --delete

      - name: Upload favicon and manifest files
        run: |
          aws s3 cp ./favicon.ico s3://${{ secrets.S3_BUCKET }}/favicon.ico \
            --cache-control "public, max-age=31536000"
          aws s3 cp ./favicon-16x16.png s3://${{ secrets.S3_BUCKET }}/favicon-16x16.png \
            --cache-control "public, max-age=31536000"
          aws s3 cp ./favicon-32x32.png s3://${{ secrets.S3_BUCKET }}/favicon-32x32.png \
            --cache-control "public, max-age=31536000"
          aws s3 cp ./apple-touch-icon.png s3://${{ secrets.S3_BUCKET }}/apple-touch-icon.png \
            --cache-control "public, max-age=31536000"
          aws s3 cp ./android-chrome-192x192.png s3://${{ secrets.S3_BUCKET }}/android-chrome-192x192.png \
            --cache-control "public, max-age=31536000"
          aws s3 cp ./android-chrome-192x192.webp s3://${{ secrets.S3_BUCKET }}/android-chrome-192x192.webp \
            --cache-control "public, max-age=31536000"
          aws s3 cp ./android-chrome-512x512.png s3://${{ secrets.S3_BUCKET }}/android-chrome-512x512.png \
            --cache-control "public, max-age=31536000"
          aws s3 cp ./android-chrome-512x512.webp s3://${{ secrets.S3_BUCKET }}/android-chrome-512x512.webp \
            --cache-control "public, max-age=31536000"
          aws s3 cp ./site.webmanifest s3://${{ secrets.S3_BUCKET }}/site.webmanifest \
            --cache-control "public, max-age=86400" \
            --content-type "application/manifest+json"

      - name: Upload HTML files
        run: |
          # Root HTML files
          aws s3 sync . s3://${{ secrets.S3_BUCKET }} \
            --exclude "*" \
            --include "*.html" \
            --cache-control "public, max-age=3600" \
            --delete
          
          # Services pages
          aws s3 sync ./services s3://${{ secrets.S3_BUCKET }}/services \
            --cache-control "public, max-age=3600" \
            --delete
          
          # Location pages
          aws s3 sync ./locations s3://${{ secrets.S3_BUCKET }}/locations \
            --cache-control "public, max-age=3600" \
            --delete
          
          # About page
          aws s3 sync ./about s3://${{ secrets.S3_BUCKET }}/about \
            --cache-control "public, max-age=3600" \
            --delete
          
          # Contact page
          aws s3 sync ./contact s3://${{ secrets.S3_BUCKET }}/contact \
            --cache-control "public, max-age=3600" \
            --delete

      - name: Invalidate CloudFront cache
        if: ${{ secrets.CLOUDFRONT_DISTRIBUTION_ID != '' }}
        run: |
          aws cloudfront create-invalidation \
            --distribution-id ${{ secrets.CLOUDFRONT_DISTRIBUTION_ID }} \
            --paths "/*"

      - name: Deployment summary
        run: |
          echo "✅ Deployment completed successfully!"
          echo "🌐 Site: https://avtreepros.com"
          echo "⏰ CloudFront cache invalidation in progress (5-15 minutes)"
